import React from "react";
import { STATIC_FOOTER_PRODUCTS } from "../../../../constants/static";

const FooterProduct = () => {
  return (
    <div>
      {STATIC_FOOTER_PRODUCTS.map((item, index) => {
        return
      })}
    </div>
  )
};

export default FooterProduct;